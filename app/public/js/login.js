if (window.location.search.includes('error=1'))                          //if URL contains error query parameter
    {
        document.getElementById('error-message').style.display ='block'; //visualize the message
    }